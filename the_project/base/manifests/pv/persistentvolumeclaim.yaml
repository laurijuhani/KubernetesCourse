apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: the-project-claim
  namespace: project
spec:
  storageClassName: project-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
