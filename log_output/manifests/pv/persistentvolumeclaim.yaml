apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-claim
  namespace: exercises
spec:
  storageClassName: log-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
